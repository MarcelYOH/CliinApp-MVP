<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Bandeau noir au démarrage/reprise (diagnostic confirmé par logs +
             captures d'écran réelles, appareil à RAM très contrainte) : ce
             thème (hérité de Theme.Black.NoTitleBar à l'origine, donc fond
             de fenêtre NOIR) s'affiche brièvement avant que Flutter n'ait
             peint sa première frame — au premier lancement ET après un
             retour au premier plan (l'OS tue souvent le process en
             arrière-plan faute de RAM, ce qui relance un démarrage à froid
             plutôt qu'une vraie reprise). windowBackground opaque CLAIR
             appliqué nativement dès la création de la fenêtre : plus aucune
             dépendance au rendu Flutter ni au timing d'un appel SystemChrome
             côté Dart. statusBarColor/navigationBarColor restent
             transparents (comportement d'origine, voir
             lib/core/utils/system_ui_helper.dart) : de nombreuses pages
             (profil groupe, détail action, création de signalement, écrans
             d'inscription...) s'appuient dessus pour laisser leur propre
             contenu s'étendre sous ces barres — les opacifier casse ce
             plein écran partout (régression déjà constatée une fois). -->
        <item name="android:windowBackground">@color/app_background</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
